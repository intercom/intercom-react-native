import React from 'react';
import {Text} from 'react-native';
import {styles} from '../styles/App.styles';

export function SettingsScreen(): React.JSX.Element {
  return <Text style={styles.title}>Settings Screen</Text>;
}